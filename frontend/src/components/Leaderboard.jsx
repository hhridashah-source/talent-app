import React, { useState, useEffect } from 'react';\nimport api from '../services/api';\n\nconst Leaderboard = ({ talentId }) => {\n  const [leaderboard, setLeaderboard] = useState([]);\n  const [loading, setLoading] = useState(true);\n  const [viewType, setViewType] = useState('talent'); // talent or global\n\n  useEffect(() => {\n    fetchLeaderboard();\n  }, [talentId, viewType]);\n\n  const fetchLeaderboard = async () => {\n    try {\n      let response;\n      if (viewType === 'talent' && talentId) {\n        response = await api.get(`/leaderboard/talent/${talentId}`);\n      } else {\n        response = await api.get('/leaderboard/global/top');\n      }\n      setLeaderboard(response.data);\n    } catch (error) {\n      console.error('Failed to fetch leaderboard:', error);\n    }\n    setLoading(false);\n  };\n\n  if (loading) {\n    return <div className=\"text-center py-8\">Loading leaderboard...</div>;\n  }\n\n  const getMedalEmoji = (rank) => {\n    if (rank === 1) return '🥇';\n    if (rank === 2) return '🥈';\n    if (rank === 3) return '🥉';\n    return '📍';\n  };\n\n  return (\n    <div className=\"bg-white rounded-lg shadow-lg p-6 max-w-4xl mx-auto\">\n      <h2 className=\"text-3xl font-bold mb-6\">🏆 Leaderboard</h2>\n      \n      <div className=\"mb-6 flex gap-4\">\n        <button\n          onClick={() => setViewType('talent')}\n          className={`px-4 py-2 rounded font-bold ${\n            viewType === 'talent'\n              ? 'bg-blue-600 text-white'\n              : 'bg-gray-200 text-gray-700 hover:bg-gray-300'\n          }`}\n        >\n          Talent Rankings\n        </button>\n        <button\n          onClick={() => setViewType('global')}\n          className={`px-4 py-2 rounded font-bold ${\n            viewType === 'global'\n              ? 'bg-blue-600 text-white'\n              : 'bg-gray-200 text-gray-700 hover:bg-gray-300'\n          }`}\n        >\n          Global Top 20\n        </button>\n      </div>\n\n      {leaderboard.length === 0 ? (\n        <p className=\"text-center text-gray-500\">No rankings available yet</p>\n      ) : (\n        <div className=\"overflow-x-auto\">\n          <table className=\"w-full\">\n            <thead className=\"bg-gray-100\">\n              <tr>\n                <th className=\"px-4 py-2 text-left\">Rank</th>\n                <th className=\"px-4 py-2 text-left\">User</th>\n                {viewType === 'talent' && <th className=\"px-4 py-2 text-center\">Videos</th>}\n                <th className=\"px-4 py-2 text-center\">Rating</th>\n                <th className=\"px-4 py-2 text-center\">Points</th>\n              </tr>\n            </thead>\n            <tbody>\n              {leaderboard.map((entry, idx) => (\n                <tr key={idx} className=\"border-b hover:bg-gray-50\">\n                  <td className=\"px-4 py-3 font-bold text-lg\">\n                    {getMedalEmoji(entry.rank || idx + 1)}\n                  </td>\n                  <td className=\"px-4 py-3\">\n                    <div className=\"flex items-center\">\n                      {entry.user?.profilePicture && (\n                        <img\n                          src={entry.user.profilePicture}\n                          alt={entry.user.username}\n                          className=\"w-8 h-8 rounded-full mr-3\"\n                        />\n                      )}\n                      <span className=\"font-semibold\">{entry.user?.username}</span>\n                    </div>\n                  </td>\n                  {viewType === 'talent' && (\n                    <td className=\"px-4 py-3 text-center\">{entry.videoCount}</td>\n                  )}\n                  <td className=\"px-4 py-3 text-center\">\n                    <span className=\"bg-yellow-100 text-yellow-800 px-3 py-1 rounded\">\n                      {entry.averageRating?.toFixed(1)}/10\n                    </span>\n                  </td>\n                  <td className=\"px-4 py-3 text-center font-bold text-purple-600\">\n                    {entry.points}\n                  </td>\n                </tr>\n              ))}\n            </tbody>\n          </table>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default Leaderboard;\n